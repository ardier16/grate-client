const mutations = {
  // root
  CLEAR_STATE: 'CLEAR_STATE',
  POP_STATE: 'POP_STATE',
}

const actions = {}

const getters = {}

export const vuexTypes = {
  ...mutations,
  ...actions,
  ...getters,
}
